[build]
buildCommand = "pnpm install --frozen-lockfile && pnpm build"

[deploy]
startCommand = "tsx server/_core/index.ts"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[deploy.variables]
RAILPACK_NO_SPA = "1"
